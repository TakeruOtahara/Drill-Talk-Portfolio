# Drill-Talk 開発ログ（Trial & Error）

## 目次
- [プロジェクトコンセプトとシステムアーキテクチャ](#プロジェクトコンセプトとシステムアーキテクチャ)
- [アーキテクチャの進化（v1 → v3）](#アーキテクチャの進化v1-v3)
- [苦労した技術的課題と解決策（v3のブレイクスルー）](#苦労した技術的課題と解決策v3のブレイクスルー)
- [今後の技術的展望（商用化へのロードマップ）](#今後の技術的展望商用化へのロードマップ)
- [過去の開発メモ・エラー解決録（アーカイブ）](#過去の開発メモエラー解決録アーカイブ)

---

## プロジェクトコンセプトとシステムアーキテクチャ

### コンセプト：メタ認知を加速させる「AI生徒」
「教えることは、二度学ぶこと（To teach is to learn twice）」 
独学者が「分かったつもり」を解消し、知識を完全に定着させるためのアウトプット特化型プラットフォーム。AI生徒「マナブ」に教えるプロセスを通じて、自分の知識の欠落（ギャップ）を可視化する。

### 1セッション「5分間」のマイクロレクチャーと評価
1.  **画像プロンプト:** 教科書1枚でスタート。問題作成の手間をゼロに。
2.  **リアルタイム・フィードバック:** WebSocket経由でマナブの理解度が変動。
3.  **マナブの「授業ノート」（差分抽出フィードバック）:**
    * サマライズ: ユーザーの説明を整理した「マナブ君のノート」。
    * 赤字の質問: 教材にあるが説明されなかった要素を「＿＿＿って何？」と明示。
    * リテイク・ループ: ノートを元に、すぐに5分間の「教え直し」が可能。

### システムアーキテクチャ概要
モダンなWeb技術とリアルタイム通信、クラウドインフラを融合させたスケーラブルな構成。

* **Frontend:** TypeScript / Next.js (App Router)
    * 状態管理と滑らかなUI演出（Lottie, Framer Motion）
    * Web Audio APIによる録音とWebSocket通信の制御
* **Backend:** Python / FastAPI
    * Gemini 2.5 Flash APIを利用したマルチモーダル解析
    * WebSocketによるステートフルなリアルタイム双方向通信
* **Infrastructure (想定):** Microsoft Azure
    * Azure Static Web Apps: フロントエンドのホスティング
    * Azure App Service: バックエンド（WebSocket有効化）の実行
    * Azure SQL Database: 学習ログとノートの永続化

---

## アーキテクチャの進化（v1 → v3）

本プロジェクトは、初期プロトタイプ（v1）から現在の本番想定モデル（v3）に至るまで、機能要件の高度化に合わせてシステムアーキテクチャを根本から見直し、リファクタリングを重ねてきました。

### 🔹 v1: 概念実証（PoC）フェーズ
* **技術スタック:** Python, Streamlit, gTTS
* **設計:** フロントエンドとバックエンドが単一のファイルに混在するモノリス構造。
* **通信:** HTTPベースのポーリングと、アクション毎の画面全体リロード。
* **課題:**
    * 音声対話のたびにStreamlitの再描画（Rerun）が走り、UX（レスポンス速度）に限界があった。
    * `gTTS` による音声合成が遅く、リアルタイム性に欠けていた。
    * チャットとドリルのロジックが密結合しており、拡張性が低かった。

### 🔹 v2: 責務分離とリアルタイム化フェーズ
* **技術スタック:** Next.js (React), FastAPI, WebSocket, edge-tts
* **設計:** クライアント（UI）とサーバー（API）を完全に分離したモダンなWebアプリケーション構成へと刷新。
* **通信:** `FastAPI` と `Next.js` 間を **WebSocket** で接続し、双方向のリアルタイム通信を実現。
* **UI/UX:** Reactの状態管理を活用し、コンポーネント単位での再描画に限定。シームレスな画面遷移を実現。
* **音声:** `edge-tts` の非同期ストリーミング処理を導入し、AIの音声応答速度を劇的に改善。
* **課題:**
    * 音声認識（STT）とAIの音声再生が被った際のエコーバック（AIの声をユーザーのマイクが拾ってしまう問題）が未解決。
    * エラーハンドリング（APIの一時的な不具合など）への堅牢性が不足。

### 🔹 v3: メタ認知AIと堅牢性（本番環境想定）フェーズ【現在の完成版】
* **技術スタック:** Next.js, FastAPI, Docker, Pydantic, Framer Motion, VAD制御
* **AIロジックのブレイクスルー（メタ認知評価）:**
    単なる「チャットボット」から脱却。「教材の4要素抽出」「ユーザーの5分間の発話ログ」「ユーザー自身の忘れたことメモ」の3点をGemini APIでクロスチェックし、【リカバリー】【ブラインドスポット】を客観的に指摘する高度な教育アルゴリズムを実装。
* **UXと音声制御の極致（VADの導入）:**
    `AudioContext` を用いた VAD (Voice Activity Detection) を自作実装。マナブ君（AI）が喋り始めた瞬間にマイク入力を物理的（`abort()`）に遮断し、エコーバックを完全に防止。
* **UIの洗練:**
    `Framer Motion` を導入し、通信状態やマイクの音量に連動してリアルタイムに動くSVGアバターと、状態遷移を示す `SquirrelLoader`（リスのUI）を実装。ユーザーの待ち時間のストレスを緩和。
* **エンタープライズ級の堅牢性:**
    * `Pydantic` を用いた環境変数（CORS設定やAPIキー）の厳格なバリデーション。
    * インフラ起因のエラー（429 Rate Limit や 503 Overloaded）に対する「指数バックオフ（Exponential Backoff）」を用いた自動リトライロジックの実装。
    * Dockerコンテナ化による、Azure等へのシームレスなデプロイ準備。

---

## 苦労した技術的課題と解決策（v3のブレイクスルー）

### 1. 非同期対話の「排他制御」と無限ループの防止（最大の技術的成果）
* **課題:** APIの返答に数秒の遅延があるため、マナブ君が質問を考えている間に先生が次の話題に進んでしまい、突然マナブ君が話を遮って割り込んでしまう問題が発覚。さらにそれが原因で音声認識が暴走（無限ループ）する危険性がありました。
* **解決策:** プロの現場でも難しい「非同期の排他制御（Mutex）」を実装。
    * **質問のキューイング:** バックエンドから質問が届いてもすぐには発声せず、一旦ストック。
    * **マイクの排他制御:** 先生の息継ぎ（無音）を検知した瞬間にマイクを強制ミュートし、その隙にマナブ君がキューに溜まった質問を発声。終わったらマイクをオンに戻す。
    * **無限ループ防止:** 発声中は新たな質問を作らず履歴に残すだけに制御。

### 2. 通信障害の解決とインフラ耐性の強化（レースコンディション回避）
* **課題:** 画像アップロード時にバックエンドが無反応になり、データ通信が Pending で止まる現象が発生。
* **分析:** WebSocketの接続完了前に、フロントが巨大データを送信しようとする「レースコンディション」が原因。
* **解決策:** フロント側に「送信キュー（待機列）」を実装。接続完了（onopen）までデータをストックし、開通後に一斉送信する堅牢なロジックに変更。
* **副産物:** API混雑時の 503 エラーに対し、バックエンドの「指数バックオフ（自動リトライ）」が正常作動することを証明。

### 3. リフレッシュに負けない「記憶力」（レジリエンスの強化）
* **課題:** WebSocket再接続時やブラウザ更新時に講義データが消失する。
* **解決策:** フロントエンドでのバックアップ保持と、再接続時のデータ同期プロトコルを実装。教材、履歴、評価ノート、メモ、**「残り時間（タイマー）」**まで `localStorage` に保存し、即座に復帰できるアーキテクチャを実現。

### 4. モバイル対応における「音声ロック」の解除
* **課題:** スマホ特有の「ユーザーのタップアクションがないと音が出ない」制約により、授業が開始できない。
* **解決策:** 「授業開始」ボタンのクリックイベント内に、マイク権限取得と同時に `AudioContext.resume()` を発火させる「魔法の1タップ」を実装し、制約を突破。

---

## 今後の技術的展望（商用化へのロードマップ）

このプロジェクトを商用レベルへと昇華させるための、今後のインフラ・セキュリティ課題と対策案です。

1.  **セッション管理の外部化とステートレス化**
    * **課題:** 現在のFastAPIサーバー内のローカルメモリによる対話履歴管理では、スケールアウト時や再起動時にセッションが消失するリスクがある。
    * **対策:** `Azure Cache for Redis` を導入。セッション情報を外部に切り出し、バックエンドをステートレス化して高可用性を実現。
2.  **API過剰消費対策の高度化（二段構えの防御）**
    * **第1ライン（インフラ）:** Azure API Management (APIM) でのIP制限・レート制限により、DoS攻撃等を遮断。
    * **第2ライン（アプリ）:** ログイン機能を導入し、ユーザーID単位の個別クォータ（利用枠）管理へ移行。
3.  **認証基盤導入と法的コンプライアンスの遵守**
    * **対策:** `Azure AD B2C` 等を採用し、個人情報保護法や GDPR 等に基づく厳格なセキュリティとコンプライアンス要件に適合させる。
4.  **インフラ・セキュリティの強化**
    * **対策:** `Azure WAF (Web Application Firewall)` を導入し、SQLインジェクションやXSS等の攻撃からアプリケーションを保護。

---

## 過去の開発メモ・エラー解決録（アーカイブ）

### [2026-03-01] バックエンド：WebSocket基盤とプロンプト分離
* FastAPIで `/ws/manabu` エンドポイント作成。
* `prompts.py` を作成し、AIの性格設定とレスポンスのテンプレートを分離。メンテナンス性とランダム性を向上。
* **エラー解決:** `ImportError` は循環参照・変数名不一致が原因。`uvicorn` のリロードで解決。

### [2026-03-01] 音声演出戦略：プリセット方式
* 毎回の音声合成API呼び出しによるレイテンシとコストを削減するため、感情ごとの高品質音声ファイルを事前にフロントに配置し、理解度スコアに基づいて即時再生する仕組みを検討。（後にエッジでの動的生成へ移行）

### [2026-03-02] ライブラリの刷新とモデル移行
* 非推奨の `google-generativeai` から最新の `google-genai` SDK へ完全移行。
* `gemini-2.0-flash` での429エラー対策として、OCR・テーマ抽出・キーワード生成を単一プロンプトにバッチ化（JSON Mode活用）。API消費を1/3に削減。
* その後、**`gemini-2.5-flash`** へ正式にアップグレードし、更なる高速化と安定性を確認。

### [2026-03-x] Docker環境構築のトラブルシューティング
* **事象:** PowerShellで `docker` コマンドが認識されない。
* **解決:** 環境変数 (Path) の反映遅延。PowerShellからシステム環境変数へ直接Dockerのbinパスを注入し、永続化。
* **事象:** `COPY` 命令で `requirements.txt` が見つからずビルド停止。
* **解決:** ローカル構造とDockerfileのパス指定の不一致を修正（`requirements.lock`の利用へ移行）。