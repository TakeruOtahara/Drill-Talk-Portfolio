# Drill-Talk 開発ログ：技術の軌跡とトラブルシューティング

## 目次
- [1. プロジェクト概要と設計思想](#1-プロジェクト概要と設計思想)
- [2. アーキテクチャの進化（v1 → v3）](#2-アーキテクチャの進化v1-v3)
- [3. 技術的ブレイクスルー：複雑な課題へのアプローチ](#3-技術的ブレイクスルー複雑な課題へのアプローチ)
- [4. 技術ログ：実装とトラブルシューティングの記録](#4-技術ログ実装とトラブルシューティングの記録)
- [5. 今後の技術的展望（商用化へのロードマップ）](#5-今後の技術的展望商用化へのロードマップ)

---

## 1. プロジェクト概要と設計思想

### コンセプト：メタ認知を加速させる「AI生徒」
「教えることは、二度学ぶこと（To teach is to learn twice）」
本アプリは、独学者が「分かったつもり」を解消し、知識を完全に定着させるためのアウトプット特化型プラットフォームです。ユーザーがAI生徒「マナブ」に教えるプロセスを通じて、自身の理解の死角（ブラインドスポット）を可視化します。

### システムアーキテクチャ概要
モダンなWeb技術とリアルタイム通信、クラウドインフラを融合させたスケーラブルな構成を採用しています。
* **Frontend:** TypeScript / Next.js (App Router)
* **Backend:** Python / FastAPI / Gemini 2.5 Flash API
* **Infrastructure (Planned/Current): Microsoft Azure (Container Apps, Key Vault, Log Analytics)

---

## 2. アーキテクチャの進化（v1 → v3）

本プロジェクトは、機能要件の高度化に合わせてシステム構成を根本から見直し、リファクタリングを重ねてきました。

### 🔹 v1: 概念実証（PoC）フェーズ
* **技術スタック:** Python, Streamlit, gTTS
* **設計:** フロントエンドとバックエンドが単一ファイルに混在するモノリス構造。
* **通信:** HTTPベースのポーリング。アクション毎の画面全体リロード。
* **課題:** 音声対話のたびに再描画（Rerun）が走り、リアルタイムなUXに限界があった。

### 🔹 v2: 責務分離とリアルタイム化フェーズ
* **技術スタック:** Next.js (React), FastAPI, WebSocket, edge-tts
* **設計:** クライアント（UI）とサーバー（API）を完全に分離。
* **通信:** WebSocketによる双方向リアルタイム通信を実現。
* **課題:** 音声認識（STT）とAI発話の衝突によるエコーバック問題が発覚。

### 🔹 v3: ゼロトラスト＆クラウドネイティブ最適化フェーズ【現在の完成版】
* **技術スタック:** Next.js (Standalone), FastAPI, Docker, Pydantic, Framer Motion, VAD制御, Bicep(IaC)
* **進化点:** 
  * **メタ認知評価:** 原本・発話ログ・メモの三者照合アルゴリズムを実装。
  * **VAD (Voice Activity Detection):** 自作の音声アクティビティ検知により、マイクとAI発話の排他制御を達成。
  * **BFF (Backend For Frontend) アーキテクチャ:** Next.jsの `middleware.ts` を用いてサーバーサイドでAPIキーを隠蔽し、ブラウザからシークレット情報を完全に排除（ゼロ・シークレット）。
  * **IaCとインフラ最適化:** Bicepを用いたインフラのコード化。Pydanticによる環境変数バリデーションと、Dockerの非root実行による本番環境（Azure Container Apps）を想定した堅牢性・軽量化を実現。
  * **FinOps（クラウドコスト最適化）:** Page Visibility APIとユーザー入力監視を用いたセッション自動切断・復帰ロジックをフロントエンドに実装。インフラの無駄な待機コストを削減。

---

## 3. 技術的ブレイクスルー：複雑な課題へのアプローチ

### 1. 非同期対話の「排他制御」と無限ループの防止
* **課題:** APIの返答遅延中、ユーザーが次の話題に進むとAIが突然割り込み、音声認識が暴走する危険性。
* **解決:** 「非同期Mutex（排他制御）」を実装。ユーザーの無音を検知した瞬間にマイクを強制ミュートし、キューに溜まった質問を安全に消化するシーケンスを構築。

### 2. 通信障害の解決（WebSocket レースコンディション）
* **課題:** 画像アップロード時、ハンドシェイク完了前にデータが送られ通信が Pending になる現象。
* **解決:** フロントエンドに「送信キュー」を実装。`onopen` 完了までデータをストックする堅牢なプロトコルへ変更。

### 3. リフレッシュに負けない「レジリエンス」の強化
* **課題:** ブラウザ更新や瞬断による講義データの消失。
* **解決:** 教材・ログ・タイマーまでを `localStorage` に毎秒保存し、再接続時に `SYNC_SESSION` でサーバー側の記憶も復元する設計を導入。

### 4. BFFパターンによるゼロ・シークレットとCSWSH完全防御
* **課題:** フロントエンド側でバックエンドのAPIキーを保持すると、悪意のあるユーザーによる直叩きや、罠サイトからのCross-Site WebSocket Hijacking (CSWSH) の標的になるリスクがあった。
* **解決:** Next.jsの `middleware.ts` をBFFとして配置。ブラウザからのリクエストの `Origin` ヘッダーを厳格に検証して不正アクセスを水際で遮断し、正規の通信にのみサーバーサイドで秘匿されたAPIキー (`X-DrillTalk-Key`) を付与してAPIMへ横流しする「ゼロトラストアーキテクチャ」を構築。

### 5. クラウド課金の最適化（FinOps）とエコ・コネクション管理
* **課題:** WebSocket通信はインフラ側（Azure Container Apps）で「処理中のリクエスト」として扱われる。そのため、ユーザーが別タブを開いたままアプリを長時間放置した場合でも、コンテナがアイドル状態に移行せず、一番高いアクティブ課金が垂れ流しになる仕様上の罠があった。
* **解決:** フロントエンドに監視ロジック（`useIdleTimeout`）を新設。`document.hidden` (Page Visibility API) による「画面の表示状態」と、物理的な操作・音声入力の有無を統合して監視。5分間「画面が見えない」または「完全無操作」状態が続いた場合、自発的にWebSocketを切断してACAをアイドル課金（深夜はゼロスケール）へ移行させる。再開時は即座に再接続し、`localStorage` から会話状態を同期（`SYNC_SESSION`）することで、UXを維持したままインフラコストの最適化を実現した。

---

## 4. 技術ログ：実装とトラブルシューティングの記録

### ISSUE 1: Dockerコマンドが認識されない（パス解決の不整合）
* **事象:** Windows環境において、Docker Desktopインストール後にPowerShellで `docker` コマンドを実行しても `CommandNotFoundException` が発生する。
* **原因の切り分け:** 1. `Test-Path` コマンドでDockerのバイナリが物理的に存在するか確認 → `True`（存在する）
  2. 絶対パスで実行 → 成功。これにより、プログラムの破損ではなくOS側の環境変数（Path）の反映遅延と断定。
* **解決策:** PowerShellから直接システム環境変数へパスを注入（`$env:Path += ";C:\Program Files\Docker\Docker\resources\bin"`）し、即時反映と永続化を実現。

### ISSUE 2: Gemini API の Rate Limit (429 Error) による機能停止
* **事象:** `gemini-2.0-flash` を利用して教材の要素抽出を行っていた際、短時間の連続リクエストにより `429 Too Many Requests` エラーが頻発し、AIの応答が停止。
* **原因:** 無料枠のクォータ制限に対し、OCR、テーマ抽出、キーワード生成を別々のAPIリクエストとして非同期で連続発火させていたため。
* **解決策:**
  1. **プロンプトのバッチ化:** 3つの処理を単一のプロンプトに統合し、JSON Mode (`response_mime_type='application/json'`) を活用して1回のリクエストで全構造化データを取得するようロジックを改修。API消費量を1/3に削減。
  2. **モデルのアップグレード:** 処理効率の向上に伴い、モデルを `gemini-2.5-flash` へ移行し、安定稼働を確認。

### ISSUE 3: モジュールの循環参照と ImportError
* **事象:** バックエンド起動時、`ImportError: cannot import name 'SYSTEM_PROMPT'` が発生しサーバーがクラッシュ。
* **原因:** `logic.py` と `prompts.py` 間での相互インポート（循環参照）、およびファイル変更後の保存漏れ。
* **解決策:** 設計を見直し、AIの性格設定や固定テキストを `prompts.py` に完全分離し、`logic.py` から単方向でインポートするアーキテクチャにリファクタリング。

### ISSUE 4: コンテナ名の競合（Naming Conflict）
* **事象:** `docker run` 実行時に "The container name is already in use" となり起動不可。
* **原因:** 過去に起動した同名コンテナが `Exited` 状態で残存しており、Dockerのライフサイクルにおいて名前が予約されたままであった。
* **解決策:** `docker rm -f <container_name>` による強制クリーンアップ手順を自動化スクリプトに組み込み解決。

### ISSUE 5: APIMとNode.js間のHTTPヘッダー大文字・小文字仕様の罠
* **事象:** BFFからAPIM（Azure API Management）へ正しいAPIキーを送信しているはずが、常に `401 Unauthorized` で弾かれる。
* **原因:** Node.jsの `Headers` オブジェクトがHTTP/2の仕様に則り、送信時にヘッダー名を自動ですべて小文字化していたのに対し、APIMのXMLポリシー側が `ignore-case="false"` （厳密な大文字小文字区別）となっていたための不整合。
* **解決策:** APIMのポリシーを緩和すると共に、カスタムヘッダーの標準規格である `X-DrillTalk-Key` へ命名規則を統一し、プロトコルレベルの仕様差異を吸収。

### ISSUE 6: クラウドネイティブに耐えうるDockerの非root化とキャッシュの罠
* **事象:** Dockerビルド時にソースコードの変更が反映されない（`Up 6 days`の状態）、およびコンテナがデフォルトの特権ユーザー（root）で実行されるセキュリティリスクが発覚。
* **解決策:**
  1. `docker-compose up -d --build --force-recreate` を用い、強力なビルドキャッシュを物理的に破棄して最新のイメージを適用。
  2. バックエンドの `Dockerfile` にて `addgroup / adduser` を実行し、権限を最小化した `appuser` を作成。エンタープライズのインフラセキュリティ基準（ゼロトラスト）を満たす設計へと昇華。

### ISSUE 7: ゼロスケールにおけるWebSocketハンドシェイクのタイムアウトとUXのジレンマ
* **事象:** コスト削減のためバックエンドを `minReplicas: 0` に設定した結果、初回接続時にコンテナの起動待ち（約15秒）が発生。HTTPリクエストとは異なり、WebSocketのハンドシェイクはこの遅延を許容できずタイムアウトエラーとなり、UX（ユーザー体験）を著しく損なう致命的な問題が発覚した。
* **アーキテクチャの検討:**
  1. **Azure Web PubSubの導入:** 真のサーバーレスWebSocket通信を確立するベストプラクティスだが、バックエンドのPub/Subアーキテクチャへの大規模な改修が必要となるため、v3.0での導入は見送り（将来課題とする）。
  2. **Azure App Serviceへの移行:** 常時稼働（Always On）前提ならApp Serviceがシンプルだが、マイクロサービス志向の喪失を意味するため却下。
* **技術的決断 (ACAの採用継続):** フロントエンドとバックエンドを同一のContainer Apps Environmentに配置することで得られる「セキュアな内部VNet」「超高速な内部DNSによるコンテナ間通信」、そして「アイドル課金（CPUが動いていない時間は極めて低コスト）」のメリットはApp Serviceを凌駕すると判断し、ACAの採用を継続。
* **解決策:** KEDA (Cronスケーラー) を導入し、営業時間（8:00-24:00）のみ常時待機させるハイブリッド戦略を実装。深夜帯のコールドスタート対策として、フロントエンド読み込み時にバックエンドへ疎通確認（Warm-up Ping）を自動実行するロジックをフロント側へ組み込み、UXの低下を最小限に抑制した。

### ISSUE 8: API課金防御とUXを両立する「Silent Drop」パターンの実装
* **事象:** インフラ境界（APIM）を削除して内部通信に切り替えたことで、仮にフロントエンドを突破された場合、バックエンドのGemini API呼び出しが連射され、課金が跳ね上がるDDoS・スパムリスクが生じた。
* **検討:** 
  1. エラー検知時にフロントエンドへ警告を返す手法も検討したが、ネットワークラグによる意図せぬ二重送信をした正規ユーザーのUXを損なうため却下。
  2. **構成の整理**: Issue 5にてAPIMのヘッダー問題を解決したが、WebSocketのステートフルな性質と、APIMを通すことによるオーバーヘッド（遅延）の回避を考慮し、BFF（Next.js）がGatekeeperの役割を完全に代替する設計へと最終的にシフトした。
* **解決策:** バックエンド（FastAPI）のWebSocket処理において、前回のメッセージから `500ms` 未満の連続送信を検知した場合、エラーを返さずに無音でリクエストを破棄する「Silent Drop（Shadow Banning）」を実装。UXを一切損なうことなく、アプリケーション層でのAPI課金防御を完遂した。

### ISSUE 9: Bicepデプロイ時のコンテナ起動タイムアウト (Operation expired)
* **事象:** Azureへのデプロイ時、Container Appsの起動が10分経過でタイムアウトし失敗。
* **原因:** System-Assigned IDを使用していたため、アプリの起動と同時に発行されたIDへKey Vaultのアクセス権限が浸透する前に、アプリがシークレット取得に走りタイムアウトを起こした。
* **解決策:** 事前に作成可能な **User-Assigned Identity** へアーキテクチャを変更。Bicepの依存関係 (`dependsOn`) を厳格に定義し、「身分証作成と権限付与」が完全に完了してからアプリを構築するデッドロック回避構成を確立。

### ISSUE 10: Azureリソースの上限とSoft Delete（ごみ箱）の罠
* **事象:** `MaxNumberOfRegionalEnvironmentsInSubExceeded`（環境数の上限）エラーや、リソースグループを消去したにも関わらず Key Vault が `ConflictError` を起こす。
* **原因:** Student枠における「1リージョン1環境」の制限。さらに、Azure Key Vaultのセキュリティ仕様である「論理的な削除 (Soft Delete)」により、削除後も同名リソースが90日間ごみ箱に保護されていたため。
* **解決策:** 古いリソースグループを整理し、CLI/ポータルから論理削除された Key Vault を物理削除（Purge）してインフラを更地化することで解決。

### ISSUE 11: 組み込みロールIDの解決と冪等性（二重登録）の担保
* **事象:** Bicepデプロイ時に `RoleDefinitionDoesNotExist`（ルール不明）や `RoleAssignmentExists`（二重登録）エラーが頻発。
* **原因:** 組み込みロールを現在のリソースグループ内で検索しようとした（`resourceId` の誤用）ことと、再デプロイのたびに権限付与の名前（GUID）が変動する実装になっていた。
* **解決策:** `subscriptionResourceId()` を用いてサブスクリプション全体の正しい組み込みロールIDを参照。さらに、権限付与の名前を `guid(対象リソースID, マネージドID, ロールID)` とリソースに紐づく一意な固定値にすることで、Bicep最大の強みである**「冪等性（何度実行しても同じ安全な結果になる性質）」**を完全担保した。

### ISSUE 12: 監視ログの「取り込みコスト」爆発リスク
* **事象:** WebSocketのPINGや正常通信（200 OK）のログが大量発生し、Log Analyticsの課金が青天井になる懸念。
* **解決策:** 
  1. **消音設計:** `main.py` のログレベルを `WARNING` に引き上げ、Dockerfileに `--no-access-log` を追加して正常系の出力を根絶。
  2. **物理防壁:** Bicepで `dailyQuotaGb: 0.16` を設定。万が一の際も月額数百円以内で課金を強制遮断する「ブレーカー」を実装。
* **結果:** 障害調査に必要なエラーログのみを抽出しつつ、平常時のログコストをほぼゼロに抑制。

### ISSUE 13: ACRストレージの肥大化と超過課金
* **事習:** 自動デプロイのたびにイメージが蓄積され、ACR Basicの10GB制限を超過するリスク。
* **解決策:** GitHub Actionsに「セルフクリーニング」ステップを実装。デプロイの最後に `tail -n +3` ロジックを実行し、最新2世代（稼働用＋戻し用）以外の古いイメージを全自動で削除。
* **結果:** 手動メンテ不要でストレージコストを一定以下に保つ、自律的なパイプラインを確立。

### ISSUE 14: 離席中のクライアント・サーバー双方の資源浪費
* **事象:** 放置されたタブによる「WebSocket維持（サーバー課金）」と「マイク監視（クライアントCPU消費）」の継続。
* **解決策:** `useIdleTimeout` フックを実装。5分間の「無操作・非表示」で通信を切断し、同時にマイク（MediaStream）も物理的に `stop()` してデバイスリソースを解放。
* **結果:** サーバーコストを削るだけでなく、ユーザー端末のバッテリー負荷も抑える「真のエコ設計」を実現。

---

## 5. 今後の技術的展望

1. **セッション管理の外部化 (Azure Cache for Redis):**
   バックエンドをステートレス化し、高可用性とスケーラビリティを両立させる。
2. **APIコストの緻密な管理:**
   Azure API Management (APIM) でのIP制限に加え、Azure AD B2C による認証を導入し、ユーザーごとの個別クォータ管理へ移行。
3. **セキュリティの強化 (Azure WAF):**
   SQLインジェクションやXSS等の攻撃からアプリケーションを保護するインフラ境界の構築。
4. **レスポンシブ・ドロワーの実装:**
   モバイル環境での操作性向上のため、メモ欄をスライド式のドロワーUIへ刷新する。
5. **Azure Web PubSub による真のサーバーレスWebSocketの実現:**
   将来的にユーザー数と同時接続数がスケールした際、現在の「ステートレスなバックエンドコンテナ」からさらに進化したPub/Subアーキテクチャへ移行する。コネクション管理をAzure Web PubSubに完全委譲し、バックエンドコンテナを純粋なイベントワーカーとして `minReplicas: 0` で運用する、究極のコスト最適化とスケーラビリティの確立を目指す。